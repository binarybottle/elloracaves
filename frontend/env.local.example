# Supabase Configuration
# Get these from your Supabase project settings (Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Cloudflare Images
# Get this from your Cloudflare dashboard (Images > Delivery URL)
# The account hash is the part after imagedelivery.net/
NEXT_PUBLIC_CF_IMAGES_ACCOUNT=your-cloudflare-account-hash

# Optional: API URL for local development (fallback for images)
# Only used when Cloudflare image IDs are not set in the database
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

